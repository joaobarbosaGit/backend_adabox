yarn add express mongosse mongodb uuid typescript -D @types/express -D ts-node-dev -D @types/uuid -D

yarn tsc --init

yarn tsc


mudar tsconfig outDir
mudar tsconfig // "strict:true
mudar em package.json 
    "scripts": {
    "dev": "ts-node-dev --transpile-only --ignore-watch node_modules --respawn src/server.ts"
    },